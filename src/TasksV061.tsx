import {useState} from 'react';import type {ModuleKey,Project} from './types';
export interface AnalysisSettings{maxMissing:number;imputation:'none'|'feature_median'}
export function TasksV061({project,run,settings,setSettings}:{project:Project;run:(m:ModuleKey)=>void;settings:AnalysisSettings;setSettings:(x:AnalysisSettings)=>void}){
 const modules:[ModuleKey,string][]=[['chemistry','实测化学成分'],['transcriptomics','转录组'],['proteomics','蛋白组'],['metabolomics','代谢组'],['microbiome','肠道菌群'],['scfa','短链脂肪酸'],['phenotype','药效与生化']],[runtime,setRuntime]=useState<Record<string,unknown>|null>(null);
 async function check(){setRuntime(window.tsrDesktop?await window.tsrDesktop.checkRuntime():{提示:'浏览器预览无法检测本机Python/R'})}
 return <section><div className="sectionHead"><div><h2>本地分析任务与参数</h2><p>桌面版按模块调用正式运行时；转录组使用DESeq2，蛋白组使用limma，缺少依赖时阻断。</p></div><button onClick={check}>检测运行环境</button></div>{runtime&&<div className="qc warning"><b>运行环境</b><span>{JSON.stringify(runtime)}</span></div>}<div className="form form2"><label>特征最大缺失率<input type="number" min="0" max="0.9" step="0.05" value={settings.maxMissing} onChange={e=>setSettings({...settings,maxMissing:Math.min(.9,Math.max(0,Number(e.target.value)))})}/></label><label>缺失值策略<select value={settings.imputation} onChange={e=>setSettings({...settings,imputation:e.target.value as AnalysisSettings['imputation']})}><option value="none">不自动填补（推荐）</option><option value="feature_median">特征中位数填补（敏感性分析）</option></select></label></div><p className="muted">参数写入结果和报告。蛋白组中位数填补不能自动解决MNAR；转录组仅接受raw counts。</p><div className="taskList">{modules.map(([k,n])=>{const a=project.assets.find(x=>x.module===k),groups=new Set(project.samples.filter(s=>s.included).map(s=>s.group)),mapped=groups.has(project.meta.controlGroup)&&groups.has(project.meta.modelGroup),enabled=!!a&&a.status!=='blocked'&&project.samples.length>=4&&mapped;return <div className="task" key={k}><i className={enabled?'ready':'locked'}/><div><b>{n}数据处理</b><span>{!a?'尚未导入数据':!mapped?'空白组/模型组尚未完成映射':a.status==='blocked'?'数据质控阻断':k==='transcriptomics'?'调用R/DESeq2；TPM/FPKM会被拒绝':k==='proteomics'?'调用R/limma经验贝叶斯模型':enabled?'可运行并记录参数':'样本数不足'}</span></div><button disabled={!enabled} onClick={()=>run(k)}>{enabled?'运行':'未启用'}</button></div>})}</div></section>
}
